carabao_mango_cnn.pth is the pretrained dataset for v1 and v2

a different pretrained dataset is in v3 (one for bruise and for ripeness)

cnn code works but will need to test for accuracy

need to add more dataset

code is separated by version. 

v1 is the original code

v2 is the latest code and so on

need to experiment on parameters
